# Clone the repository to a local directory
git clone https://github.com/DrJefff/amazon-fba-website.git

# Change to the local repository directory
cd repository-name

# Stage the changes to the code and templates
git add .

# Commit the changes to the local repository
git commit -m "Update the code and templates for the HTTP website for Amazon FBA selling"

# Push the changes to the remote repository on GitHub
git push
